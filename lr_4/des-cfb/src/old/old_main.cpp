#include <iostream>

#include "DES.h"
#include "ConstantsPP.h"

int main (int argc, char* argv[])
{
    /*
    std::array<char, 8> T = {1, 2, 3, 4, 5, 6, 7, 8};

    for (int i = 0; i < 8; ++i)
    {
        printBinary(std::cout, T[i]) << '\n';
    }
    std::cout << '\n';

    std::array<char, 8> T0 = initialPermutation(T);

    std::cout << '\n';

    for (int i = 0; i < 8; ++i)
    {
        printBinary(std::cout, T0[i]) << '\n';
    }
    
    //using namespace Constants;
    std::array<char, 8> TT = permutation(T0, Constants::IP_reverse);

    std::cout << '\n';

    for (int i = 0; i < 8; ++i)
    {
        printBinary(std::cout, TT[i]) << '\n';
    }
    */

    return 0;
}